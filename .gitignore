# Exclude local Terraform caches and modules
**/.terraform/

# Prevent committing Terraform's internal state files
*.tfstate
*.tfstate.*
terraform.tfstate.backup

# Crash log files
crash.log

# Exclude any manually created or automatically generated variable files
*.tfvars
*.tfvars.json

# Local-only override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Prevent committing Terraform execution plans
*.tfplan

# Exclude JSON files (if needed, but be cautious as some Terraform files are JSON)
# *.json

# Exclude IDE-specific files (optional, but recommended)
.idea/
.vscode/
*.swp
*.swo
